// const renderError = await (task , userId , isAuth) => {

//     const {id, title, desc} = task;
//     const details = await getDetails(userId);

//     return res.status(400).render('edit-task', {
//           title: 'Edit Tasks',
//           task: {id, title, desc},
//           isAuth: isAuth,
//           details,
//           errorMsg: 'Title Cannot be null!',
//         });

// }

// module.exports = {
//     renderError
// }
